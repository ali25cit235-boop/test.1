name: Windows 11 RDP (Optimized & Lag-Free)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
    - name: Enable Remote Desktop & Optimize Performance
      run: |
        # Enable RDP
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
        Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
        
        # User Password Setup
        net user runneradmin pass123 /add
        net localgroup administrators runneradmin /add
        
        # Performance Tuning (Disable Visual Effects for Zero Lag)
        Set-ItemProperty -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\VisualEffects' -Name "VisualFXSetting" -Value 2

    - name: Start Persistent Tunnel (Serveo)
      run: |
        Write-Host "=========================================="
        Write-Host "🚀 WINDOWS RDP READY FOR MSTSC"
        Write-Host "USERNAME     : runneradmin"
        Write-Host "PASSWORD     : pass123"
        Write-Host "=========================================="
        
        while ($true) {
            ssh -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -R 0:localhost:3389 serveo.net
            Start-Sleep -Seconds 3
        }
